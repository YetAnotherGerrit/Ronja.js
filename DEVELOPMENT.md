# Dev Manual

Just some notes from dev'in around.

## DB Migrations

```bash
# Runs all migrations - defined in package.json
yarn dev:migrate

# Generates a new migration
yarn dev:migrate:new <e.g. default-values-modulename>
```
